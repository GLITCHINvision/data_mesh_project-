default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST', 'localhost') }}"
      user: dbt_user
      password: dbt_password
      port: 5432
      dbname: data_mesh
      schema: analytics_inventory
      threads: 4
